import {Box, styled} from "@mui/material";

export const StyledRegistrationBox = styled(Box)(({theme}) => ({
    width: '100%',
    minHeight: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
}))